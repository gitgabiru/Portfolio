#include <bits/stdc++.h>

using namespace std;

int main()
{
    int a1, a2, k1, k2, n;
    int min = 0, max = 0, aux_min, aux_max;

    cin >> a1 >> a2 >> k1 >> k2 >> n;

    aux_min = (a1 * k1) + (a2 * k2);

    cout << min << endl;

    return 0;
}